---
version: 1
slug: "src-app-page-tsx"
primary_target: "src/app/page.tsx"
related_targets: ["src/components/OfferDossierScroll.tsx"]
---

# Home pillars section

Mode: Persuade (homepage band). Primary: `src/components/OfferDossierScroll.tsx` / `src/app/page.tsx`. Content: `offerCards` + `pillars` in `src/content/site.ts`.

## Scope
Keep the three Design / Supervision / FF&E dossier cards. Redesign only the six commitments underneath.

## Direction contract
THESIS: Services stay as three tall dossier plates; commitments become a second, clearly smaller horizontal strip — not a 2×3 column ledger.
OWN-WORLD: Parchment Atelier — beige-2 band, Instrument Serif titles, Graphite body, grey-200 top rules on compact tiles, shell-aligned track.
STORY: Visitor locks Design / Supervision / FF&E in photos, then browses Schedule → Implementation sideways.
FIRST VIEWPORT (band): Unchanged three cards filling the shell; below, shell-aligned snap strip of six compact proof tiles.
FORM: Horizontal proof strip (board `horizontal-strip`).
FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance
